## MODIFIED Requirements

<!-- None - This is an implementation-only refactor.

All requirements from the base spec remain unchanged:
- Container lifecycle management
- Resource limits enforcement
- Workspace mapping
- Error handling for Docker daemon unavailability

Only the underlying Docker connection mechanism changes (socket/SSH → TCP).
No sandbox behavior requirements are modified.
-->
